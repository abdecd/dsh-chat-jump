//#region src/index.ts
/** Stable Cordis plugin name. */
const name = "chat-jump";
/** The widget is client-side only: the host injects nothing. */
const inject = [];
/** No host-side effects to install. */
function apply(_ctx) {}
//#endregion
export { apply, inject, name };
